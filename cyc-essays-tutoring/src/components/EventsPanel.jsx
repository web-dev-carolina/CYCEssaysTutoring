import React, { useState, useEffect } from "react";

function EventsPanel(props) {
//<button onClick={e => {e.preventDefault(); console.log(props.events);}}>click for events!</button>
    return (
        <div className="App">
            
            
        </div>
    );
}

export default EventsPanel;